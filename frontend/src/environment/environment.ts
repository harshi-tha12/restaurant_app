// Production environment settings
// Replace baseUrl with your deployed app domain (use https)
export const environment = {
  production: true,
  baseUrl: 'https://foodierestqr.netlify.app/' // <-- change this to your deployed URL
};