// Production environment settings
// Replace baseUrl with your deployed app domain (use https)
export const environment = {
  production: true,
  baseUrl: 'https://fffoooddiiee.netlify.app/.com' // <-- change this to your deployed URL
};