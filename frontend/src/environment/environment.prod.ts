// Development environment settings
// You can leave baseUrl empty to use window.location.origin at runtime,
// or set it to your local/ngrok URL for testing (e.g. "http://localhost:4200" or "https://abcd1234.ngrok.io")
export const environment = {
  production: false,
  baseUrl: '' // e.g. 'http://localhost:4200' or leave '' to use window.location.origin
};